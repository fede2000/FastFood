@model int

@if (Model > 0)
{
    <span id="cart-count" class="cart-count-container">
        <span class="cart-count-badge visible">@Model</span>
    </span>
    
}
else
{
    <span class="cart-count-badge" style="display: none;"></span>
}